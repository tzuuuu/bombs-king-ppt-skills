[metadata]
name = powerpoint-native-render-cli
version = 0.1.0
description = Native Microsoft PowerPoint render gate CLI

[options]
packages = find:
python_requires = >=3.9
install_requires =
    PyMuPDF>=1.24.0

[options.entry_points]
console_scripts =
    powerpoint-native-render = powerpoint_native_render.cli:main
